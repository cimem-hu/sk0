import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hu.cimem.sk0-front.app',
  appName: 'sk0-front',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
